import java.util.ArrayList;

/**
 * Write a description of class ControlPrestamos here.
 * Clase controlar los usuarios
 * @author (monicamrmda) 
 * @version (16/02/2023)
 */
public class ControlPrestamos
{
    public boolean registrar(Prestamos prestamo){
       boolean res=true;
       return res;
    }
   public boolean actualizar(Prestamos prestamo){
       boolean res=true;
       return res;
    }
   public ArrayList Prestamos (String prestamos){
       ArrayList <Prestamos> lista = new ArrayList<>();
       return lista;
   }
   public boolean eliminar(){
         boolean res=true;
        return res;
}
}
